import React from 'react';
import ReactDom from 'react-dom';

const searchBar = () => {
  return <input />;
};

export default searchBar;
//ReactDom.render(<searchBar/>, document.querySelector('body'));
